import SliderImg from "./images/mainslide.jpeg";

const Slider = () => {
  return (
    <section className="slider">
      <img className="slider-img" src={SliderImg} alt="" />
    </section>
  );
};

export default Slider;
